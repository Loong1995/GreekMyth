# 待人工确认决策清单（decisions）

> Step A 产出 4/4。首批条目。写给数值/玩法策划看，不需要读代码。
> 每条含【提案】与【确认状态】。确认后该条即为规则，写入对应机制文档。
>
> **2026-07-05 人工批复**：D-02 消耗规则修改、D-03 三处拍板、D-04 修改 k 值与
> 同回合释放规则、D-05/D-06 确认，其余各条全部按提案确认。详见各条【确认状态】。
>
> 说明：任务书正文提到「附录 A」，但 `phase1_battlecore.md` 中并无独立附录 A 章节；
> 本清单按正文中所有标注「Step A 提案」的点 + 任务书要求提案的公式 + 分析中发现的
> 规则冲突整理，如有遗漏的附录请补发。

## 一、任务书明确要求 Step A 提案的规则

### D-01 石化「受到伤害 +10%」所在乘区位置

【提案】放入**易伤乘区**（旧公式中已存在的「受到伤害增加」乘区，上限 +100%），
与其他易伤效果**加法叠加**后整体结算。
即：石化目标受到的伤害 = 正常伤害 × (1 + 10% + 其他易伤加成)，总易伤不超过 +100%。

理由：① 复用已标定乘区，不新增乘区、不破坏已校准的数值上限体系；② 加法叠加避免
「石化 × 其他易伤」乘法失控；③ 对物理/谋略/真伤一视同仁（石化是物理性的僵直，
真伤也应吃满 10%）。
备选方案（若希望石化更克制物理阵容）：仅对物理伤害生效——不推荐，规则复杂化。

【确认状态】✅已确认（2026-07-05）：按提案，放易伤乘区、加法叠加。

### D-02 犹豫状态完整规则

任务书 5.3 已定：可叠加、延后回合数 = 层数。旧笔记（readme.txt）写「不可叠加刷新」，
**以任务书为准**。以下补齐细则：

1. **判定时点**：武将行动窗口开始时，若身上有犹豫（N 层），对本次「普攻 + 主动战法」
   整体 roll 一次是否延后（几率 = 施加犹豫的战法配置的触发概率，默认 50%，可配）。
   roll 中则本次行动的普攻与主动战法都延后 N 回合生效；roll 不中则正常行动。
2. **消耗规则（人工修订版）**：犹豫**不因判定 roll 而清空**。与其他状态一致按
   回合计次消耗：武将每次行动窗口结算完毕后（若有延后生效的行动，先补结算延后行动，
   再计次）犹豫执行一次持续回合数递减，到期整体移除。持续期间每次行动都要做一次
   延后判定，延后回合数 = 当前层数。
3. **延后生效**：被延后的行动登记为「延迟行动」，在 N 回合后该武将行动窗口的最前
   执行（先补执行延迟行动，再进行当回合正常行动判定）。事件表达为
   `skill_trigger(kind=delayed, delay_rounds=N)`，实际生效时发 `kind=release`。
4. **边界情况**（配 Step B3 交互矩阵逐格测试）：
   - 施法者在延迟期间**阵亡** → 延迟行动作废，不生效（阵亡清理原则一致）。
   - 施法者在生效时点**被缄默/冥锁/石化** → 主动战法部分作废，普攻部分：缄默不禁
     普攻则照常补打，冥锁/石化则一并作废。
   - 原目标在生效时点**已阵亡** → 按该战法的目标规则**重新选目标**；无合法目标则作废。
   - 延迟**跨越局边界** → 战时状态不跨局，随局清空作废（任务书已定）。
   - 追击不受犹豫影响（追击挂在普攻命中后，普攻被延后则追击自然随之延后）。

【确认状态】✅已确认（2026-07-05）：第 2 条按人工修订——消耗改为「行动时
（延后生效结算后）执行回合计次」；其余按提案。

【二次人工修订（2026-07-05）】推翻「可叠加、延后回合数=层数」：
1. 延后**固定 1 回合**：第 N 回合被延迟的行动在 N+1 回合该武将窗口最前释放；
2. 释放后才进入新一轮犹豫判定（补结算的行动本身不再做犹豫判定）；
3. 重复施加犹豫为**刷新不叠层**（重置持续计次，stacks 恒为 1）；
4. 已登记的延迟行动不受后续犹豫刷新影响（仍按原定 N+1 回合释放）。
已按此实现并更新 hesitation.md 与全部相关测试。

### D-03 单挑胜负公式与拒绝几率公式

任务书约束：按武力差随机，武力差 ≥10 时必定结果。设 `d = 高武力 - 低武力`（d ≥ 0，
同武力时不触发单挑的「双方最高」比较用站位先后破平，见 D-08）。

【拒绝几率（已定）】低武力方拒绝概率 = `d × 8%`，d≥10 时封顶 **80%**
（即武力差 10 以上仍保留 20% 接受概率，为单挑演出保留出场机会）。

【胜负公式（已定）】接受后，高武力方胜率 = `50% + d × 5%`，d≥10 时 100% 必胜。
d=0 时 50/50，线性过渡，武力差 10 必定结果，符合任务书。

【惩罚与频次（已定）】负者四维立即 -10。**一个系列只单挑一次，发生在第 1 局开局**；
惩罚**只存在第 1 局**（不带入第 2 局及以后——事件流中以 `attr_change(scope=game)`
表达）。第 2 局起不再判定单挑。

【确认状态】✅已确认（2026-07-05）：拒绝封顶 80%（差 10 以上保留 20% 接受）；
胜率公式按提案；单挑仅第 1 局一次、惩罚仅第 1 局有效。

### D-04 连携触发概率 k

任务书 5.4：主将神谕释放后，两副将自带主动战法各有 k 几率立即释放。

【已定】k = **70%**（暂定值，平衡期可调），固定配置于全局规则（`mechanics/assist.md`），
不随属性浮动。两副将各自独立 roll；触发顺序按站位顺序；准备类战法无需准备直接释放
（任务书已定）；连携释放**不占用**该战法当回合的正常释放机会——本回合轮到该副将
行动时，同一主动战法**仍可再次正常释放**。

【确认状态】✅已确认（2026-07-05）：k=70%；连携后本回合仍可继续释放同战法。

## 二、旧 core 数值/规则疑点（按任务书 4.3 记录，不擅自修改）

### D-05 兵力系数分母用全局 10000 而非武将自身上限

旧公式：出手方兵力系数 = 0.4 + 0.6 × (当前兵力 / 10000)，分母固定 10000。
未来出现 30000 兵 NPC 时系数可达 2.2（无截断），远超 1.0 设计区间。
【提案】保留旧公式字面行为（10000 固定分母、不截断）迁移。

【确认状态】✅已确认（2026-07-05）：保留、**不加截断**——超编 NPC 打出超高兵力
系数是设计意图（就是要让超编 NPC 有压迫感）。

### D-06 打满 8 回合按「剩余兵力多者胜」还是「平局进入下一局」

旧 core：打满回合上限后按双方剩余总兵力判胜负。任务书 5.1：打满未分胜负 = 平局，
残血进入下一局。两者直接冲突。
【提案】以任务书为准：**单局打满即平局**，剩余兵力只用于系列打满 7 局仍平时的
系列级展示（系列平局不强判胜负）。旧行为不迁移。

【确认状态】✅已确认（2026-07-05）：按新任务书执行。

### D-07 每队人数校验

旧 core 强制每队恰 3 人；任务书允许 1~3 人。
【提案】新 core 按 1~3 人校验；主将必须在场。

【确认状态】✅已确认（2026-07-05）。

### D-08 各类并列破平规则（确定性要求）

【提案】全项目统一破平链：**站位序（0→2）→ 队伍序（A→B）**。适用于：速度相同的
行动顺序、单挑「武力最高」并列、目标权重并列等一切需要破平处。速度相同时不 roll
先手（旧 core 速度差 0 是 50% roll——保留该 roll，仅「同队同速」站位破平；
即跨队同速仍 50/50 roll，符合旧标定）。

【确认状态】✅已确认（2026-07-05）。

### D-09 行动先手的伪随机补偿废除

旧 core 给速度竞速挂了「连败补偿」伪随机系统，但其记账键包含回合号，每回合都是
新账本，补偿从未实际累计——等价于普通随机，白增复杂度。
【提案】新 core 先手判定用普通随机（概率锚点不变：差 0→50%、1→55%、5→70%、
10→80%、≥20→100%）。战法触发类伪随机补偿（保底机制)保留且修正为真累计。

【确认状态】✅已确认（2026-07-05）。

### D-10 数值以配置为准的两处文档冲突（仅备案，无需决策）

- 雷霆神谕触发概率：以代码配置 **70%** 为准（旧 DESIGN_V2/skills.md 写 60% 为过期）。
- 蛇杖庇护治疗系数：以代码配置 **智力×1.0** 为准（skills.md 写 0.6 为过期）。
如需改回文档值属于平衡性调整，请另行提出。

【确认状态】备案知悉即可

## 三、事件流契约关键选型（详见 schema 文档，此处摘要请求确认）

### D-11 战报结构：嵌套 games[] 而非扁平流

一个战报 = 一个系列，内部按局分组；`seq` 全系列连续。论证见契约 §2.1。

【确认状态】✅已确认（2026-07-05）。

### D-12 事件分级：审计事件不进客户端战报

旧 core 每局产出数千条内部判定事件；新契约只含 21 种「播放必需」事件（单局约
350~1200 条）。内部判定走服务端 debug 通道（replay_dump 全量档）。

【确认状态】✅已确认（2026-07-05）。

### D-13 折叠/展开双 id 机制（group_id + parent_seq）

`group_id` 定义演出折叠单元（普攻与其追击是两个单元），`parent_seq` 定义因果链
（追击的组根指回普攻的伤害事件）。客户端两种粒度均零计算。论证见契约 §3.2。

【确认状态】✅已确认（2026-07-05）。

### D-14 三条省体积规则

空相位不发 phase_start；无状态变化不发事件（免疫/拒绝刷新静默）；局末批量状态清理
不逐条发事件（由 game_end 语义覆盖）。若策划要求「抵抗/免疫」有演出，后续加法式
新增 `status_resisted` 事件类型即可，不破坏契约。

【确认状态】✅已确认（2026-07-05）。

## 四、Step B3 实现期补录决策（2026-07-05，按既定原则推导，请审阅）

> 以下各条为 B3 实现中遇到的规则空白，按「任务书优先 → 已批复决策类推 → 旧 core
> 语义 → 最简一致」原则拍板并已实现+配测试。如需改判请指出条号。

### D-15 准备中的武将仍可普攻；release 免犹豫判定

准备型战法只锁「该战法的效果结算」，武将准备期间行动窗口照常普攻（旧 core 蓄力语义）。
release 段不再做犹豫判定——该行动已因准备延迟过一次，不叠加二次延迟（D-02 精神）。

【确认状态】待审阅（已按此实现）

### D-16 连携的随机与记账口径

连携 roll（k=70%）用**普通随机**、不走伪随机补偿、不触碰该战法的伪随机记账
（D-04「不占用正常释放机会」的自然延伸——记账也不占用）。副将准备型主动被连携时
跳过准备直接结算 release 效果（任务书已定）。

【确认状态】待审阅（已按此实现）

### D-17 追击的两条边界

① 普攻该击已把目标打死 → 追击不 roll 不触发（无可追之敌）；② 攻击者在普攻结算后、
追击分发前获得 forbid_basic/forbid_pursuit（如美杜莎凝视反制石化）→ 追击被封锁。
「禁普攻即禁追击」贯穿整个结算窗口而非仅窗口开始快照。

【确认状态】待审阅（已按此实现）

### D-18 海神三叉戟（旧 core 仅描述）震荡链判定口径

「逐次 70%」解释为**串行链、首败即停**（roll 中→结算→再 roll 下一次，最多 3 次）；
首个震荡目标必须异于本次受击目标，后续目标不得与本链已震荡者重复；震荡为固定量
（原伤害 50%）、不暴击不吃乘区；震荡不再触发海神（防递归）。

【确认状态】待审阅（已按此实现）

### D-19 赫尔墨斯神谕（旧 core 仅描述）数值定标

扰心印记：敌方全体；每次行动窗口开始 50% 施加犹豫（刷新不叠层；持续 2 回合、
延迟判定率 50%）。**印记仅前 2 回合生效**（持续 2 回合，覆盖第 1、2 回合行动窗口
后到期；2026-07-05 人工修订）。数值为标杆暂定值，平衡期可调（同 D-04 性质）。

【确认状态】✅已人工修订确认（2026-07-05）：仅前 2 回合生效

### D-20 阿喀琉斯之怒触发口径（人工新增标杆的细则）

「每次暴击时触发」解释为：**任何来源的伤害暴击**（普攻/战法/追击/状态响应）均触发，
不限普攻；治疗暴击不触发。追加伤害本身不可暴击、不再触发本效果；「无视统帅」
实现为**计算属性差时将对方统帅直接置 0**（属性差 = 自身有效武力全额生效，
其余乘区照常；2026-07-05 人工修订，原「跳过属性差乘区/按基准 100」作废）；
每回合上限 3 次随 round_start 重置。

【确认状态】✅已人工修订确认（2026-07-05）：无视统帅 = 对方统帅置 0

### D-21 战神怒火【血战】施加范围

「全场血战」按字面对**敌我双方全体**生效（物理易伤+30%/物理暴击率+20% 是对称的
乱战氛围，非单方增益）。我方最高武力者额外获得战神之勇（武力+5/速度+5）。

【确认状态】待审阅（已按此实现）

### D-22 选人过程事件化（target_select，schema 1.1.0，B4）

应人工要求（选人逻辑与动态受击率需在 all 版日志中可见），每次受击率选人
（候选池 + 当时受击点数 + 命中者）以**可选字段** `target_select` 挂在携带该次
选人的 `normal_attack` / `skill_trigger` / `damage` 事件 payload 上——契约加法式
演进，schema_version 升 1.1.0，客户端可忽略（向前兼容义务）。文本日志仅 all 档
打印（选人属冗余判定信息，brief 档面向客户端反演主干）。机制文档
`docs/mechanics/targeting.md`。

【确认状态】待审阅（已按此实现，2026-07-05）

### D-23 新 golden 用例集（B4 冻结入库）

9 份 golden 覆盖各机制类型阵容：3v3 纯普攻、1v1 单挑、skills（B2 原语）、
stalemate（7 局系列平局）、npc（30000 兵超编）、standard×2 种子（B3 全机制）、
oracle×2 种子（神谕连携+犹豫）。生成器 `battle/tools/gen_golden.py`（默认只对比
报差异，`--write` 才落盘）；回归 `battle/tests/test_golden.py` 逐字节比较。
golden 变更必须显式重跑生成器并在 commit message 说明原因（任务书 9）。

【确认状态】待审阅（已按此实现，2026-07-05）

## 五、Phase 2 客户端决策（C 系列，Step A 提案，待人工确认）

### C-01 屏幕布局与卡牌站位

**横屏 + 卡牌上下占位**（2026-07-05 人工修订确认）：敌方 3 卡横排在上、
我方 3 卡横排在下（主将居中），中央留横向演出通道（突进为纵向短位移 + 弹道
斜向/纵向表达；cut-in 横向全屏）。卡牌比例约 3:4（竖卡）；左右两侧留 UI 位：
左上回合指示 + 系列比分，右下播放控制条。上下构图的优点：6 张卡横向空间充裕、
兵力条/状态栏横排不拥挤，且与主流卡牌对战（炉石/Snap 场上区域）心智一致。

【确认状态】✅已人工确认（2026-07-05）：横屏，卡牌上下占位

### C-02 单挑独立演出规格

暗场切独立擂台（双方立绘对峙 + 武力值对比条）→ 接受则三次交锋剪影 + 胜负定格 +
败者四维-10 逐条飘字；拒绝则挑衅/摇头短演出。全程数据仅取 duel_challenge /
duel_result / attr_change 事件。时长：接受 ≤4.0s（超通用模板预算，单挑每系列
最多 1 次、是核心爽点，申请独立预算）；拒绝 ≤2.0s。规格详见 vfx_templates.md T6。
**单挑演出允许付费采购资源**（2026-07-05 人工确认）：特效/斩击/音效走 Asset Store
小额采购，清单与放置规范见 `docs/client/assets_upload_guide.md` §三（原 to_purchase.md 并入）；资源到位前 T6 用
程序化占位（剪影+速度线）先行实现，到位后换装不改代码。

【确认状态】✅已人工确认（2026-07-05）：4.0s 预算 + 付费资源

### C-03 飘字风格与数字缩略规则

10000 兵量级**不缩略**，直接显示原始整数（如 -1300）：四位数在卡牌宽度内可读，
缩略（1.3k）损失打击感且与兵力条数字不一致。伤害=白字（暴击=金黄大号+描边+
弹跳），治疗=绿字，DoT tick=小号暗红，属性变化=「武力 -10」蓝/红小字。
同一目标连续飘字纵向堆叠上浮，间隔 0.1s 错帧。字体后备：TextMeshPro 数字图集。

【确认状态】✅已人工确认（2026-07-05）：按提案

### C-04 跳过播放的表现

**提案：不播关键帧摘要**。跳过（至局末/至系列结果）= 镜像静默快进 + 直接显示
结算面板；面板上已含逐局比分与武将统计（数据取 result，零事件解析），足够回顾。
理由：关键帧摘要（如逐回合定格 0.3s）实现成本高、观感廉价；后续若需要，可在
B4 用「逐回合 seek」能力低成本补做。

【确认状态】✅已人工确认（2026-07-05）：按提案

### C-05 模板时长预算终值

单体 ≤1.2s（目标 1.0s，连击第 2 击 0.7s）；全体 ≤2.0s；大招 cut-in ≤3.0s
（cut-in 段 ≤1.5s，同武将同战法每局最多 1 次全长，之后降级）；单挑 ≤4.0s；
2x 倍速全模板时长×0.5 但顿帧不缩放。超时保险：任何演出超预算 2 倍强制收敛。

【确认状态】✅已人工确认（2026-07-05）：按提案

### C-06 状态特效扩展机制（自行决定，备案）

`StatusVfxTable`（status_id → 图标/施加特效/常驻特效）为唯一扩展入口；
宙斯雷霆全队雷电环绕 = thunder 的常驻特效配置项。未配置状态仅显示图标。
战法映射查表失败兜底 T1 默认模板 + 告警，播放永不中断。

【确认状态】按此实现，无需确认（机制性小决策）

### C-07 「自带机制触发」伤害的默认表现（2026-07-05 人工修订）

判定用纯事件结构：伤害组根为 `status_tick`（非主动非追击）时，**来源卡牌不做
突进/挥砍动画**，默认播 T7「天降特效伤害」（上方特效坠落至目标 + 目标头顶
来源头像标 0.6s + 飘字）；组根为 normal_attack / skill_trigger(cast/release/
assist) 的主动/追击伤害照常走卡牌动作模板。逐战法/状态可覆盖默认演出
（模板+参数+头像标开关），配置入口 SkillVfxTable / StatusVfxTable，
维护手册 `docs/client/vfx_config.md`。

【确认状态】✅已人工修订确认（2026-07-05）

### C-09 B2 实现方式备案（自行决定）

1. **代码 shader 代替 Shader Graph**：CardFX.shader 单文件集成视差/呼吸/foil/
   闪白/状态色调——文本可 diff、AI 可维护，视觉无差异；如需 2D 光照再评估迁移。
2. **卡牌为代码工厂构建**（CardView.Create），暂不固化 prefab：占位期结构
   频繁变化，代码构建维护成本最低；B3 演出模板落地后固化为 prefab 资产。
3. **震屏用自研相机抖动**（真实时间衰减噪声）代替 Cinemachine Impulse：demo
   场景尚无 Cinemachine 相机 rig；B3 搭 cut-in 镜头时一并迁移 Impulse。
4. 占位立绘 9 张由用户提供，放置 Assets/Resources/Portraits/<hero_id>.png，
   走 art_pipeline.md 管线，换正式立绘零代码。

【确认状态】按此实现，备案待追认

### C-10 B3 实现方式备案（自行决定）

1. **特效三层回退**：SkillVfxTable/StatusVfxTable 配置 key → VfxLibrary 查采购包
   prefab（VfxPlayer 池化）→ 缺失回退 VfxKit 程序化原语 + 一次性告警。任何配置
   缺失播放不中断（对齐 C-06 纪律）。
2. **配置资产由编辑器菜单生成**（GreekMyth→Build Vfx Configs，VfxConfigBuilder）：
   默认条目代码内维护、可重复执行且不覆盖 Inspector 人工修改；避免手写 YAML。
3. **三包分工**：Cartoon Coffee 刀光斩击（T1/T6）、Vefects Flipbook 命中爆炸电击
   （命中层/常驻光环）、2D Cartoon/Anime 四色系施法天降增益（T2/T3/T4/T7）。
4. **cut-in 用代码序列**（黑幕+速度线+立绘三段横扫）代替 Timeline 资产：时长短
   （≤1.5s 预算内）且需按英雄动态换立绘；Cinemachine 迁移仍留待 B4 评估。
5. **音效维持合成占位**（用户指示 sound 暂缓采购）：SfxService 11 键程序合成，
   Resources/SFX/<key>.wav 放文件即替换；修复场景缺 AudioListener 的无声问题。
6. **战斗 UI 用 OnGUI 最小集**：验收期工具型 UI（倍速/粒度/跳过/比分），正式
   UI 框架（UGUI/UIToolkit）留待 B4 与结算面板一并落地。

【确认状态】按此实现，备案待追认

## 六、遗留问题备案

- 任务书引用的 `reference/golden_replays/` 目录在仓库中不存在；现以
  `battlecore/tests/output/`（32 份）与 `battlecore/battlecore/references/`（1 份）
  作为旧样例来源。若另有 golden 存档请提供。
- 「连击」触发下的追击：提案为**每次普攻独立判定追击**（任务书 5.4 已定），连击
  第二击的追击与第一击互不影响；同一追击战法可在同回合触发两次。已按此写入契约示例。
