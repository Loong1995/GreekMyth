# GreekMyth 项目规则（.cursorrules）

## 项目一句话背景
希腊神话题材 2D 卡牌对战手游；服务器权威结算（Python `battle/`），
客户端(Unity `Assets/Scripts/ClientBattle/`)只按事件流播放、零结算。

## 根本上下文（开工必读）
**`docs/discipline/`**：全项目不可违背的规范与全局规则总纲
（project_overview 世界观术语 / global_rules 机制红线 / coding_standards /
doc_standards / ai_workflow_pitfalls 踩坑录）。任何工作先读该目录 index.md；
踩新坑当次会话追记 pitfalls。

**自动注入规则**：`.cursor/rules/`（`00-session-start.mdc` 始终生效；
ClientBattle / battle / docs 按 glob 叠加）。规则要求开工第一条工具调用
用 Read 打开权威文档——比「口头必读」更可靠。

## 当前阶段
- Phase 3（机制大修 + 武将池 v3.1 + 性格系统）**已完成**；
  v3.2 追加（减免施加序逐实例判定、圣盾反弹）已落地。
  schema **1.4.1** / core **battle-0.4.1**（`battle/version.py`）。
- Phase 4（势能/连发协击/BGM 分层/经理人战术等，`docs/prompts/phase4_coreupgrade.md`）
  **已执行完毕**（2026-07-19~20 落地，见 changelog），武将池 32 将。
- 客户端 **ClientBattle** 框架（2026-07-09 建立，占位可跑）已于 2026-07-23
  完成播放核心架构重构（控制器/建造器/导演/策略拆分），主文档
  `docs/client/index.md`；行为规格 `playback_requirements.md`、
  架构权威 `architecture.md`。

## 契约文件（唯一前后端共同依赖）
- `docs/schema/battle_events.md`（总纲）+ `battle_events_payloads.md`（明细）
  + `battle_events.schema.json`（机器可校验）
- 冻结后仅允许加法式演进（新增事件类型/新增可选字段），禁止修改已有字段语义

## 目录约定
- `docs/discipline/` 根本规范；`docs/prompts/` 任务书只读存档；`docs/schema/` 契约；
  `docs/dev/` 决策/changelog/历史存档；`docs/mechanics/` 机制(index+每机制一文件)；
  `docs/skills/` 战法三段式；`docs/client/` 客户端文档
- `battle/` 战斗 core（tests/ 单测+golden、tools/、benchmarks/）
- `Assets/Scripts/ClientBattle/` 客户端唯一实现（5 层，asmdef 隔离）
- `battlecore/`、`reference/` **只读，禁止修改其中任何文件**

## 编码红线（细则见 docs/discipline/global_rules.md）
- 确定性最高优先级：单一 RNG 流、种子外部注入；禁止系统时间/无序遍历等不确定源；
  所有遍历集合必须有显式排序规则；数值一律整数/定点（概率与系数用 bps 万分比）
- 战法以 `docs/skills/` 描述与 D 系列决策为标杆语义，只能照描述实现
- 禁止为通过测试修改已标定公式或 golden；golden 变更必须显式更新并说明原因
- core 内部错误不得静默吞掉：结算异常必须使战斗失败并输出完整上下文，禁止半截战报
- 客户端零结算、零死帧、播放单元完整（group_id 全量聚合）、占位三级回退
- 每个文档文件 ≤500 行，超出拆分并在 index 登记；文档与代码不同步视为任务未完成
- 每个工作会话结束追加 `docs/dev/changelog.md`（日期 + 3-5 行）

## 属性术语（定论，见 docs/discipline/project_overview.md §四）
force=武力/Might、intelligence=智力/Hex（谋略伤害与治疗同用此字段）、
command=统率/Guard、speed=敏捷/Speed；四维即此四字段。
