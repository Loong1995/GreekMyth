---
description: 厂包/画廊特效接入——强制先读标准化纪律，禁止裸奔与旁路落盘
globs: Assets/Editor/GreekMyth/**/*Vfx*.cs,Assets/Editor/GreekMyth/Wire*.cs,Assets/Editor/GreekMyth/Standardize*.cs,Assets/Scripts/ClientBattle/VFX/**/*.cs,Assets/Scripts/ClientBattle/Units/**/*Aura*.cs,Assets/Scripts/ClientBattle/Units/**/*Shroud*.cs,Assets/Scripts/ClientBattle/**/*Shroud*.cs,docs/client/vfx_*.md,docs/client/assets_upload_guide.md
alwaysApply: false
---

# 特效接入工作流（不可裸奔、不可旁路）

用户**点名画廊序号 / 厂包 EffectN /「参考某件观感」**，或改**命中 / 弹道 /
光环 / 罩身 / 单挑 / 地面**任一 VFX key 时，**动手前必须 Read**：

1. `docs/client/vfx_standardization.md`（清单权威；无 GUI 晋升；§〇.1 旁路禁令）
2. `docs/client/vfx_mobile_budget.md`（**移动端预算红线 + 观感需求替代方案表**；
   接任何一件都成立，不需要人再提"要考虑性能"）
3. 按需：`docs/client/assets_upload_guide.md`（key 登记）、
   `docs/discipline/extension_points.md`（挂载点）、
   相关机制文档（如 `docs/mechanics/duel.md`）

## 强制步骤（对照 standardization §四）

1. **定件**：`python battle/tools/_gallery_index_dump.py 包:件`
   （包号以人点名的为准；**[1/8]=我方标准件**，其后才是厂包。
   **禁止**用「分母 61→Magic=2/8」这类启发式覆盖口头包号——标准件目录
   现亦约 61 件，会把 1/8 件 41 错接到 Effect19（P-71））
2. **落盘（唯一入口）**：`VfxPackStandardizer.Standardize(src, key, usage)`
   - 定点/卡面 → `VfxUsage.Anchor`；地面 → `Ground`；**罩身 → `Shroud`**；
     不挂卡的全场氛围 → `AmbientField`
   - 若点的是 **1/8 我方标准件**，key 已在 Resources → **只改接线**，禁止再
     Standardize 覆盖同名 key
   - 接线脚本＝清单 + AutoHeal + 菜单（参考 `WireDuelStageVfx` /
     `WireAresMightShroud`）
3. **禁止 Play 模式接线**；运载器定点用途直接点 `*_Collision`/`*_Explosion`
4. **登记** `assets_upload_guide.md` + Profile / `StagePerformanceConfig` key
5. **验收** Verify / 控制台无 MissingComponent；changelog 一行

## 移动端纪律：只降强度，不删效果（P-79）

上下文永远是移动端。流水线已自动执行，接件时**核对体检结果**：

**可调层（永不删，挂 `VfxTierScale` 按 `VfxQuality` 档位缩放）**：
屏幕折射、粒子发射量、灯亮度。成品保留厂包满强度，缩放在运行期。
唯二例外：① 罩身用途的折射一律中和（糊卡面＝读不到战况，正确性问题 P-77）；
② 第 2 盏起的实时灯只在高端档点亮（调亮度省不下开销，只能开/关）。

**摘掉的只有不成立/污染的机制**（不算"关效果"）：
Projector 与厂包 Decal（URP 根本不渲染，替代＝自研裂地，**必须登记**）、
粒子碰撞/触发（舞台无碰撞体）、`WindZone` / `CameraShake` / `PerPlatformSettings`。
**件自带音源一律保留**：素材音与画面同拍，属观感的一部分；`SfxManager` 管的是
战斗语义音，两者叠加，要静音走音量总线，禁止在落盘期摘。

**玩家侧**：档位是设置面板里的自动/低/中/高，**不做"关闭某类特效"的开关**。
**编辑器一致性**：编辑器按 `VfxQuality.EditorTier`（默认 Mid）跑同一套系数。

想要的观感 → 做法对照表见 `vfx_mobile_budget.md` §四
（扭曲＝保留折射+冲击环+震屏+裂地；焦痕→自研裂地；更炸→大颗少量+升档+顿挫）。

## 禁止（P-70 / P-77 / P-79）

- Profile/演出代码直指 `Assets/KriptoFX/**` 等包目录
- 凭印象手搓「像 EffectN」的粒子
- 做人点的晋升 GUI / 队列
- 只改 key 字符串却不落盘 Resources 标准件
- **任何平行落盘旁路**：`CopyFull` / `InstantiatePrefab`+`SaveAsPrefabAsset` /
  手拷 Resources / 「完整件原样拷贝不走流水线」——一律违规（P-77）。
  「接近画廊」靠流水线用途分支（如 `Shroud` 摘折射），不靠跳过清洗
- 罩身成品残留 Distortion / PerPlatformSettings / 死贴花
- **摘件自带音源**（2026-07-28 人工定案）
- **为省性能把某层从成品里摘掉**（除死层/污染层外）——一律改为档位降强度
- 把档位系数**烤进 prefab**（中高端机永久损失，改平衡点要重接所有件）
- 给玩家做「关闭某类特效」的开关；只允许强度档
- 只在某个用途分支里执行纪律而不加体检项——等于没有这条纪律（P-79）
